name: roamly
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.3
  sqflite: ^2.3.3
  path: ^1.9.1
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  http: ^1.2.1
  image_picker: ^1.1.2
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/utils/country-by-continent.json
